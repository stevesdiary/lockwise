export interface PlanFeatures {
  access?: string[];           // e.g. ['access_code', 'exit_code']
  amenities?: string[];        // e.g. ['swimming_pool']
  services?: string[];         // e.g. ['customer_support', 'group_chat']
}
